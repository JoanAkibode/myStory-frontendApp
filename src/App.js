import { AuthProvider } from './context/AuthContext';

function App() {
    return (
        <AuthProvider>
            {/* Your app routes/components */}
        </AuthProvider>
    );
}

export default App; 